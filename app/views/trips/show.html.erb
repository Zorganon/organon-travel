<p id="notice"><%= notice %></p>
<div class="container trip-box">
  <%= render @trip %>
    
  <%= link_to 'Edit', edit_trip_path(@trip) %> |
  <%= link_to 'Back', trips_path %>
  
  <%= render 'new_comment' %>
  <hr>
  <div class="row show-page-product-detail">
    <%= render 'comments' %>
  </div>
</div>
